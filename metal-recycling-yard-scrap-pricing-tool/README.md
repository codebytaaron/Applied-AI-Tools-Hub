# Scrap Pricing Tool

Industry: **Metal Recycling Yard**

## What it does
Optimizes pricing and intake.

## Run
```bash
python -m venv .venv
source .venv/bin/activate
pip install -r requirements.txt
cp .env.example .env
python main.py
```

Runs in MOCK_MODE by default.
