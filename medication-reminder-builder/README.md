# Medication Reminder Builder

Veterinary clinic AI template.

## What it does
Creates medication schedules and reminder text.

## Run
```bash
python -m venv .venv
source .venv/bin/activate
pip install -r requirements.txt
cp .env.example .env
python main.py
```

Runs in MOCK_MODE by default.
