# Provenance Tracker

Industry: **Antique Dealer**

## What it does
Tracks item history and authenticity.

## Run
```bash
python -m venv .venv
source .venv/bin/activate
pip install -r requirements.txt
cp .env.example .env
python main.py
```

Runs in MOCK_MODE by default.
