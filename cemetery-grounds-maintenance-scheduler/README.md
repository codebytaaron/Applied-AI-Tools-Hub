# Maintenance Scheduler

Industry: **Cemetery Grounds**

## What it does
Plans landscaping and upkeep.

## Run
```bash
python -m venv .venv
source .venv/bin/activate
pip install -r requirements.txt
cp .env.example .env
python main.py
```

Runs in MOCK_MODE by default.
