# Restoration Planner

Industry: **Clock Repair Shop**

## What it does
Documents restoration workflows.

## Run
```bash
python -m venv .venv
source .venv/bin/activate
pip install -r requirements.txt
cp .env.example .env
python main.py
```

Runs in MOCK_MODE by default.
