# KPI Scoreboard

Niche company template: **Escape Room Venue**

## What it does
Defines KPIs, targets, and a weekly scoreboard template.

## Run
```bash
python -m venv .venv
source .venv/bin/activate   # Windows: .venv\Scripts\activate
pip install -r requirements.txt
cp .env.example .env
python main.py
```

Notes:
- Runs in MOCK_MODE by default.
- Edit `prompt.py` to customize behavior.
