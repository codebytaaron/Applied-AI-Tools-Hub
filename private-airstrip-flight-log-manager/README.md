# Flight Log Manager

Industry: **Private Airstrip**

## What it does
Manages arrivals, departures, and fuel.

## Run
```bash
python -m venv .venv
source .venv/bin/activate
pip install -r requirements.txt
cp .env.example .env
python main.py
```

Runs in MOCK_MODE by default.
