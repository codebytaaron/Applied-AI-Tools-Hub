# Moving Company Load Planner

Industry focus: **Local Businesses**

## What it does
Plans truck loads and routes.

## Run
```bash
python -m venv .venv
source .venv/bin/activate
pip install -r requirements.txt
cp .env.example .env
python main.py --in examples/input.txt --out out.md
```
