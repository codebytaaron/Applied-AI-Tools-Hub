# Yoga Studio Intro Sequence

Industry focus: **Local Businesses**

## What it does
Creates intro email sequences.

## Run
```bash
python -m venv .venv
source .venv/bin/activate
pip install -r requirements.txt
cp .env.example .env
python main.py --in examples/input.txt --out out.md
```
