# Pet Grooming Appointment Optimizer

Industry focus: **Local Businesses**

## What it does
Optimizes booking slots and staffing for grooming salons.

## Run
```bash
python -m venv .venv
source .venv/bin/activate
pip install -r requirements.txt
cp .env.example .env
python main.py --in examples/input.txt --out out.md
```
