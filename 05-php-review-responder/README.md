# php-review-responder

PHP CLI template that drafts review responses with tone control.

This repo is intentionally written in **PHP** (not Python/JS) so GitHub shows a different language mix.

## Run
```bash
php main.php
```

## Input format
Put your notes in `input.txt` (or pipe stdin). Output is printed to stdout.
