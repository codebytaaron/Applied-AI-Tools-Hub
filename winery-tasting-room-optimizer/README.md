# Tasting Room Optimizer

Industry: **Winery**

## What it does
Optimizes tasting flow and upsells.

## Run
```bash
python -m venv .venv
source .venv/bin/activate
pip install -r requirements.txt
cp .env.example .env
python main.py
```

Runs in MOCK_MODE by default.
