# Bow Fitting Assistant

Industry: **Archery Shop**

## What it does
Documents fitting and tuning steps.

## Run
```bash
python -m venv .venv
source .venv/bin/activate
pip install -r requirements.txt
cp .env.example .env
python main.py
```

Runs in MOCK_MODE by default.
