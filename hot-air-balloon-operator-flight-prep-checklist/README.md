# Flight Prep Checklist

Industry: **Hot Air Balloon Operator**

## What it does
Prepares weather and safety checks.

## Run
```bash
python -m venv .venv
source .venv/bin/activate
pip install -r requirements.txt
cp .env.example .env
python main.py
```

Runs in MOCK_MODE by default.
