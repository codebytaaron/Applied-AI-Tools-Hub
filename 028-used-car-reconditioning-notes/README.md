# Used Car Reconditioning Notes

Industry focus: **Local Businesses**

## What it does
Structures recon notes.

## Run
```bash
python -m venv .venv
source .venv/bin/activate
pip install -r requirements.txt
cp .env.example .env
python main.py --in examples/input.txt --out out.md
```
