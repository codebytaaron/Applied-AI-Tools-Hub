# Boarding Management System

Industry: **Horse Stable**

## What it does
Manages feeding and turnout schedules.

## Run
```bash
python -m venv .venv
source .venv/bin/activate
pip install -r requirements.txt
cp .env.example .env
python main.py
```

Runs in MOCK_MODE by default.
