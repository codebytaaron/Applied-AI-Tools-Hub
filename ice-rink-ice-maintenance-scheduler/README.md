# Ice Maintenance Scheduler

Industry: **Ice Rink**

## What it does
Schedules resurfaces and repairs.

## Run
```bash
python -m venv .venv
source .venv/bin/activate
pip install -r requirements.txt
cp .env.example .env
python main.py
```

Runs in MOCK_MODE by default.
