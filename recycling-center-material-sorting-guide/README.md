# Material Sorting Guide

Industry: **Recycling Center**

## What it does
Improves sorting accuracy and compliance.

## Run
```bash
python -m venv .venv
source .venv/bin/activate
pip install -r requirements.txt
cp .env.example .env
python main.py
```

Runs in MOCK_MODE by default.
