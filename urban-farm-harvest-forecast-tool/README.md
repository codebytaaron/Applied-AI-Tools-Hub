# Harvest Forecast Tool

Industry: **Urban Farm**

## What it does
Forecasts yields and labor needs.

## Run
```bash
python -m venv .venv
source .venv/bin/activate
pip install -r requirements.txt
cp .env.example .env
python main.py
```

Runs in MOCK_MODE by default.
