# Crop Rotation Planner

Industry: **Greenhouse Nursery**

## What it does
Plans seasonal plant rotations.

## Run
```bash
python -m venv .venv
source .venv/bin/activate
pip install -r requirements.txt
cp .env.example .env
python main.py
```

Runs in MOCK_MODE by default.
