# sql-kpi-dictionary-agent

SQL-first template that defines KPI dictionary tables and insert examples.

Language: **SQL** (not Python or JavaScript)

## Run
```text
Run in SQLite/Postgres with small edits
```

## Input
Edit `input.txt` with your notes. Output prints to stdout (or produces SQL schema).
