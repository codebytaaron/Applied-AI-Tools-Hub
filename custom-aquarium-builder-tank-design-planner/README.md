# Tank Design Planner

Industry: **Custom Aquarium Builder**

## What it does
Designs builds and maintenance plans.

## Run
```bash
python -m venv .venv
source .venv/bin/activate
pip install -r requirements.txt
cp .env.example .env
python main.py
```

Runs in MOCK_MODE by default.
