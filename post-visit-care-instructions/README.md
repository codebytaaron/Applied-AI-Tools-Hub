# Post-Visit Care Instructions

Veterinary clinic AI template.

## What it does
Generates after-care instructions for pets.

## Run
```bash
python -m venv .venv
source .venv/bin/activate
pip install -r requirements.txt
cp .env.example .env
python main.py
```

Runs in MOCK_MODE by default.
