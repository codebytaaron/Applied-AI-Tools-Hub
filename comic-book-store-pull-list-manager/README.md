# Pull List Manager

Industry: **Comic Book Store**

## What it does
Manages subscriptions and inventory.

## Run
```bash
python -m venv .venv
source .venv/bin/activate
pip install -r requirements.txt
cp .env.example .env
python main.py
```

Runs in MOCK_MODE by default.
