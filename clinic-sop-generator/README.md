# Clinic SOP Generator

Veterinary clinic AI template.

## What it does
Turns clinic procedures into clean SOPs.

## Run
```bash
python -m venv .venv
source .venv/bin/activate
pip install -r requirements.txt
cp .env.example .env
python main.py
```

Runs in MOCK_MODE by default.
