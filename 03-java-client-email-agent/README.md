# java-client-email-agent

Java CLI template that produces a professional client email draft.

This repo is intentionally written in **Java** (not Python/JS) so GitHub shows a different language mix.

## Run
```bash
javac src/Main.java
java -cp src Main
```

## Input format
Put your notes in `input.txt` (or pipe stdin). Output is printed to stdout.
