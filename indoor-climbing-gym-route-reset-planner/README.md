# Route Reset Planner

Industry: **Indoor Climbing Gym**

## What it does
Plans route rotations and staff assignments.

## Run
```bash
python -m venv .venv
source .venv/bin/activate
pip install -r requirements.txt
cp .env.example .env
python main.py
```

Runs in MOCK_MODE by default.
