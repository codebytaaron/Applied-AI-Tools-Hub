# lua-menu-description-polish

Lua CLI template that rewrites menu descriptions.

This repo is intentionally written in **Lua** (not Python/JS) so GitHub shows a different language mix.

## Run
```bash
lua main.lua
```

## Input format
Put your notes in `input.txt` (or pipe stdin). Output is printed to stdout.
