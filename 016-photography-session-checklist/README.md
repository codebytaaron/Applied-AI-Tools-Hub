# Photography Session Checklist

Industry focus: **Local Businesses**

## What it does
Creates shoot-day checklists.

## Run
```bash
python -m venv .venv
source .venv/bin/activate
pip install -r requirements.txt
cp .env.example .env
python main.py --in examples/input.txt --out out.md
```
