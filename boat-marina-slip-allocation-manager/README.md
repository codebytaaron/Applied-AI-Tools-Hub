# Slip Allocation Manager

Industry: **Boat Marina**

## What it does
Manages slip usage and contracts.

## Run
```bash
python -m venv .venv
source .venv/bin/activate
pip install -r requirements.txt
cp .env.example .env
python main.py
```

Runs in MOCK_MODE by default.
