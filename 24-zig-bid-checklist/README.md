# zig-bid-checklist

Zig CLI template that generates a bid submission checklist.

This repo is intentionally written in **Zig** (not Python/JS) so GitHub shows a different language mix.

## Run
```bash
zig run main.zig
```

## Input format
Put your notes in `input.txt` (or pipe stdin). Output is printed to stdout.
